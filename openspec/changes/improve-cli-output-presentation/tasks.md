## 1. Foundation

- [x] 1.1 Add a centrally-managed `Spectre.Console` package reference to `Vara.Cli` and verify `dotnet build` succeeds
- [x] 1.2 Create a `Vara.Cli.Tests` project (mirroring the other `tests/Vara.*.Tests` projects) with a `Spectre.Console.Testing` reference, wired into the solution, and verify `dotnet test` discovers and runs an initial placeholder test
- [x] 1.3 Publish the real `Vara.Cli` project AOT (`dotnet publish -r win-x64 --self-contained`) after adding the package reference and verify zero AOT/trim warnings and record the actual published binary size delta against today's baseline (0 MB immediately after adding the unused reference; re-measured at the end of section 6 with real usage in place: **+1.16 MB** - 6.80 MB → 7.96 MB, less than the ~2 MB estimate since trimming removes the Spectre features we don't use)

## 2. Shared presentation conventions (`cli-presentation`)

- [x] 2.1 Implement a small shared helper (or extension on `AnsiConsole`) exposing the three outcome-severity styles (clean success, partial failure, hard error) so every command applies them consistently, and verify with a `Vara.Cli.Tests` case per style using `TestConsole`
- [x] 2.2 Verify (via `TestConsole` configured as redirected/non-interactive, and separately with `NO_COLOR` set) that styled output degrades to plain, uncolored text per the `cli-presentation` spec's fallback scenarios
- [x] 2.3 Manually verify on a plain Windows `conhost` window (not just Windows Terminal) that colored/styled output renders correctly, per design.md's conhost VT risk (confirmed by user: colored output renders correctly on plain Windows `cmd`/`conhost`)

## 3. Backup progress display rework

- [x] 3.1 Spike whether `AnsiConsole.Live`'s `AutoRefresh` redraws on its own cadence without an explicit `Refresh()` call or task-value change; confirm whether `ProgressHeartbeat`'s `Timer` can be removed outright or whether a minimal `ctx.Refresh()`-calling timer is still required, and record the outcome (spiked: `LiveDisplay` - unlike `Progress` - has no `AutoRefresh` at all, only `ctx.Refresh()`/`UpdateTarget()`; a minimal timer calling `ctx.Refresh()` on the existing `Live` instance is required, replacing `ProgressHeartbeat`'s snapshot-replay role since the renderable recomputes elapsed-time-based values fresh on every `Refresh()`)
- [x] 3.2 Implement the two-row `Live` composite (row 1: `Spectre.Console.ProgressBar` sized to the console profile's width; row 2: a borderless fixed-column `Grid` rendering bytes transferred/total, throughput, and ETA from `BackupProgressCalculator`'s `ProgressSnapshot`), and verify with a `Vara.Cli.Tests`/`TestConsole` case that a value crossing a digit-count boundary does not shift the other fields' horizontal position (implemented as `BackupProgressPanel`; row 1 is a hand-drawn ASCII bar, not `Spectre.Console.ProgressBar`, which turned out to be an internal type - see design.md's updated decision)
- [x] 3.3 Wire the composite into `BackupCommand`, retaining `ProgressDisplayGate`'s monotonic "never regress" guard, and updating or removing `ProgressHeartbeat` per the 3.1 spike outcome (removed `ProgressHeartbeat` entirely; a minimal `Timer` calling `ctx.Refresh()` replaces it, wired in `BackupCommand.RunWithLiveDisplay`)
- [x] 3.4 Verify the bar falls back to plain, non-progress-bar output (per `cli-presentation`) when the terminal width cannot be determined (for example, redirected output), with a `TestConsole` case (extracted the decision into `OutputMode.IsLiveCapable`, tested both branches)
- [x] 3.5 Implement color-coded run-summary reporting in `BackupCommand`/`BackupRunSummaryFormatter` (clean success vs. partial failure vs. hard error, per the `progress-reporting` delta), and verify with a `Vara.Cli.Tests` case for each of the three outcomes (clean success and partial failure verified here via `BackupOutcomeReporter`; the hard-error case is `ErrorReporting`'s path, verified in task 5.2)
- [x] 3.6 Run the existing `Vara.Application.Tests` `Reporting` suite (`BackupProgressCalculatorTests`, `ProgressDisplayGateTests`, `ProgressHeartbeatTests`, `ProgressHeartbeatStallSimulationTests`, `BackupRunSummaryFormatterTests`) and update any test that assumed a now-removed `ProgressHeartbeat`/`ProgressDisplayGate` member, verifying the suite passes (`ProgressHeartbeatTests`/`ProgressHeartbeatStallSimulationTests` removed along with the class; remaining 17 tests pass)

## 4. Snapshot and history listings

- [x] 4.1 Convert `SnapshotsCommand`'s output to a `Spectre.Console.Table` with headers, preserving the existing "no snapshots recorded" plain message for the empty case, and verify with a `Vara.Cli.Tests` case
- [x] 4.2 Convert `HistoryCommand`'s output to a `Spectre.Console.Table` with headers, and verify with a `Vara.Cli.Tests` case

## 5. Prompts and error styling

- [x] 5.1 Replace `RestoreCommand`'s `Console.ReadLine`-based overwrite prompt with `AnsiConsole.Confirm(...)`, keeping the existing `Console.IsInputRedirected` gate that decides whether to prompt at all unchanged, and verify the interactive/non-interactive/`--force` scenarios in `snapshot-history`'s existing restore requirements still behave identically (manual verification, since `RestoreCommand`'s profile/service setup isn't easily unit-testable without a real profile) - verified end-to-end against a real profile: non-interactive (no prompt, clear stderr error, exit 1) and `--force` (no prompt, overwrites) both behave identically to before; the true interactive prompt-and-answer case needs **your** manual check in a real terminal, since this sandboxed shell always reports `Console.IsInputRedirected = true`
- [x] 5.2 Update `ErrorReporting.Run` to render its error message in the hard-error style to standard error, and verify with a `Vara.Cli.Tests` case that the styled output still writes to stderr (not stdout) and preserves the existing `Error: <message>` text and exit code of 1 (made the target console injectable for testing; also confirmed end-to-end with the real built CLI that errors land only on stderr, stdout stays empty)
- [x] 5.3 Update `PruneCommand`'s completion message to use the shared success styling from section 2, and verify with a `Vara.Cli.Tests` case

## 6. Final validation

- [x] 6.1 Run the full test suite (`dotnet test`) across all projects and verify all tests pass (258 tests passed: 21 Vara.Cli.Tests, 95 Vara.Application.Tests, 49 Vara.Core.Tests, 81 Vara.Infrastructure.Tests, 12 Vara.IntegrationTests)
- [x] 6.2 Manually run `vara backup <profile>` against a real profile with mixed file sizes and verify visually: bar on its own line, no jitter in the stats line, correct color on completion (ran against a real profile with mixed file sizes 13 B-500 KB; correct byte totals, formatting, and green success summary text confirmed - the *visual* bar/color rendering needs **your** confirmation in a real terminal, since this sandboxed shell's stdout is always redirected, so it only exercises the plain-text fallback path)
- [x] 6.3 Manually run `vara backup` with a failing file (for example, a file locked by another process) and verify the amber partial-failure styling appears (reproduced with a locked file end-to-end; summary correctly lists `Failed (1): - locked.txt` and takes the partial-failure code path - visual amber color needs **your** confirmation in a real terminal, same redirected-stdout limitation as 6.2)
- [x] 6.4 Manually run `vara backup > out.txt` (redirected) and verify plain, uncolored, non-progress-bar output with no stray escape sequences in `out.txt` (verified: `out.txt` contains zero ESC (0x1B) characters, plain multi-line progress text, no bar attempted)
